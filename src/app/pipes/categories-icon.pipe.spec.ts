import { CategoriesIconPipe } from './categories-icon.pipe';

describe('CategoriesIconPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriesIconPipe();
    expect(pipe).toBeTruthy();
  });
});
